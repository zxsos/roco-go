# 远行商人货单探测(临时测试模式)

> 这是临时测试模式,**结束后整体删除**(清单见 §4)。结论沉淀到 `docs/data.md` 或新建
> `docs/pcap-<日期>.md`,不要往 `docs/data.md` 里堆。

## 1. 背景与结论

远行商人的货单由第三方 API(`/api/merchant/goods-list?...`,见 `internal/server/merchant.go`
的 `merchantFetch`)返回,**第三方自己有缓存**:轮次整点(8/12/16/20)切换后,货单往往**滞后**
才从上一轮切成新轮。实测两轮:

- 2026-08-30 20:00 轮:整点后约 **56 分钟**才切(情形 C 量级)。
- 2026-08-31 20:00 轮:整点后约 **6 分 10 秒**才切(情形 B 量级)。

滞后波动极大(6~56 分钟),按单日数字写死滞后窗口很脆 —— 下轮若又 56 分,小窗口会固化
上一轮旧货并误报。因此**不做滞后测量/窗口调参**,改用无脑高频回源。

## 2. 现行实现(`-merchant-probe`,见 `internal/server/merchant_probe.go`)

整点后**每 10 秒回源一次**,直到:

1. 拿到「本轮」货单(商品 `time_label` 以当前槽起点开头,如 `"20:00-24:00"` 对槽起点 `"20:00"`)
   → 触发订阅提醒并停砸;或
2. 整点后 **30 分钟**仍无本轮响应 → 放弃。

要点:

- 回源幂等(`merchantFetch` 每次覆盖写槽),多砸几次无害,纯靠 token 量把滞后窗口压扁。
- **不**用指纹比对、不写探测日志、不发滞后汇总邮件、没有 90 分钟窗口 —— 旧版那套测量逻辑
  已全部废弃(见 git 历史)。
- 「本轮」用 `time_label` 前缀匹配当前槽起点区分,避免整点瞬间第三方仍返回上一轮货单时被
  误判为成功、提前停砸而漏掉本轮。`time_label` 是 API 既有字段,只做前缀匹配,不是新的测量策略。
- 探测在独立 goroutine 跑,不阻塞服务;探测期间 `merchantEnsure` 的 15 分钟定时补查照常,
  两者互补(回源幂等、通知按商品/按槽去重,不会重复打扰)。
- `merchant.go` / `merchant_notify.go` **已无探测相关短路**(旧版独占回源、免冷却认领已删)。

模式:

- `auto`:服务启动即对准下一个档期整点(8/12/16/20)等待开砸;距下一档 >6h(当天四档已过)则
  放弃,提示用 `now` 手动测。
- `now`:把「现在」当整点立即开砸,随时手测一轮(无第三方依赖时也能跑通流程)。

## 3. 仍待验证

- 第三方缓存滞后到底是「按档期整点失效」还是「按请求时间 + TTL」?若是后者,每 10s 回源会
  不断刷新 TTL,可能反而拖长滞后 —— 需要对比「探测窗口内首次拿到本轮」的耗时与对照组。
- `time_label` 字段是否在所有轮次稳定返回、格式是否恒为 `"HH:MM-HH:MM"`。
- 是否需要给「本轮已响应」加一个最小间隔,避免整点前一刻上一轮货单恰好也以当前槽起点开头的
  边界误判(目前四档起点互不相同,无此风险)。

## 4. 删除清单(测试结束后)

- `internal/server/merchant_probe.go` 整个文件
- `cmd/rocom-capture/main.go` 的 `-merchant-probe` flag 与 `StartMerchantProbe` 调用
- 本文件 `AI_merchant_probe.md`
